<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    
    <!-- 회원가입 -->
    <entry key="insertUser">
        INSERT INTO USERS (
            USER_NUM, USER_ID, USER_PW, USER_NICKNAME, USER_EMAIL, USER_PHONE, USER_BIRTH
        ) VALUES (
            USERS_SEQ.NEXTVAL, ?, ?, ?, ?, ?, ?
        )
    </entry>
    
    <!-- 로그인 검증 -->
    <entry key="getUserById">
        SELECT USER_NUM, USER_ID, USER_PW, USER_NICKNAME, USER_STATUS 
        FROM USERS 
        WHERE USER_ID = ? 
          AND USER_STATUS != 'W'
    </entry>

</properties>