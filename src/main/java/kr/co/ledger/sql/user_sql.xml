<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    
    <!-- 회원가입 -->
    <entry key="insertUser">
        INSERT INTO USERS (
            USER_NUM, USER_ID, USER_PW, USER_NICKNAME, USER_EMAIL, USER_PHONE, USER_BIRTH
        ) VALUES (
            SEQ_USERS.NEXTVAL, ?, ?, ?, ?, ?, ?
        )
    </entry>
    
    <!-- 로그인 -->
    <entry key="getUserById">
        SELECT USER_NUM, USER_ID, USER_PW, USER_NICKNAME, USER_STATUS 
        FROM USERS 
        WHERE USER_ID = ? 
          AND USER_STATUS != 'W'
    </entry>

    <!-- 아이디 찾기 -->
    <entry key="findUserId">
        SELECT user_id 
        FROM users 
        WHERE user_email = ? AND user_phone = ? AND user_birth = ?
    </entry>
	
    <!-- 임시 비밀번호 발급 -->
    <entry key="updateTempPw">
        UPDATE users 
        SET user_pw = ? 
        WHERE user_id = ? AND user_email = ? AND user_phone = ? AND user_birth = ?
    </entry>
	
	<!-- 마이페이지 -->
	<entry key="getUserInfo">
    	SELECT USER_ID, USER_NICKNAME, USER_EMAIL, USER_PHONE, USER_BIRTH
    	FROM USERS 
    	WHERE USER_ID = ?
	</entry>

	<!-- 개인정보 수정 -->
	<entry key="updateUserInfo">
    	UPDATE USERS 
    	SET USER_PW = ?, USER_NICKNAME = ?, USER_EMAIL = ?, USER_PHONE = ?, USER_BIRTH = ?
    	WHERE USER_ID = ?
	</entry>
	
	<!-- 회원탈퇴 -->
	<entry key="withdrawUser">
    	UPDATE USERS 
    	SET USER_ID = ?, 
        	USER_PW = ?, 
        	USER_NICKNAME = ?, 
        	USER_EMAIL = ?, 
        	USER_PHONE = ?, 
        	USER_BIRTH = ?, 
        	USER_STATUS = 'W'
    	WHERE USER_ID = ?
	</entry>
	
</properties>